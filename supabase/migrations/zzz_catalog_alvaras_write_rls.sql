-- Substituído por supabase/migrations/zzz_p0_supabase_security.sql (P0).
-- Mantido vazio para ambientes que já executaram a versão anterior com políticas authenticated_*.
-- A migração P0 faz DROP dessas políticas e aplica escrita restrita via portal_has_edit().

select 1;
