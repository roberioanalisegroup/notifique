/** Fragmento reutilizado em listagens e detalhe de tarefas (embedded empresa + responsável). */
export const COMPANY_IN_TASK_SELECT = `id, cnpj, razao_social, nome_fantasia, situacao_cadastral, municipio, uf, responsible_user_id, responsible:profiles ( id, display_name )`;
